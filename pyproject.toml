[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
 
[project]
name = "codepaste"
version = "0.1.0"
 
[project.scripts]
codepaste = "codepaste:main"
 
[tool.setuptools.package-data]
codepaste = ["files/*"]
 