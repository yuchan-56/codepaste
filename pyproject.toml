[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"
 
[project]
name = "codepaste"
version = "0.1.0"
 
[project.scripts]
codepaste = "codepaste:main"
 
[tool.setuptools.package-data]
codepaste = ["files/*"]
 